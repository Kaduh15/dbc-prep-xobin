def eh_palindromo(frase: str) -> bool:
    """Verifica se uma frase é um palíndromo, desconsiderando espaços e maiúsculas.

    Args:
        frase (str): frase a ser verificada.

    Returns:
        bool: True se a frase for um palíndromo (ignorando espaços), senão False.
    """
    raise NotImplementedError
