def palpites_para_acertar(numero: int, tentativas: list[int]) -> int:
    """Conta quantos palpites foram necessários até acertar o número secreto.

    Args:
        numero (int): número secreto a ser adivinhado (0 a 10).
        tentativas (list[int]): sequência de palpites dados pelo jogador.

    Returns:
        int: quantidade de palpites até o primeiro acerto (1-base). Se nunca
        acertar, retorna o total de tentativas (todos os palpites foram usados).
    """
    raise NotImplementedError
