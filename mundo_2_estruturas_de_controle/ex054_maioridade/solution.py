def contagem_maioridade(anos_nascimento: list[int], ano_atual: int) -> tuple[int, int]:
    """Conta quantas pessoas já atingiram a maioridade (>= 18 anos) e quantas não.

    Args:
        anos_nascimento (list[int]): anos de nascimento de cada pessoa (ex.: 7).
        ano_atual (int): ano de referência atual, usado para calcular a idade.

    Returns:
        tuple[int, int]: (quantidade de maiores, quantidade de menores).
    """
    raise NotImplementedError
