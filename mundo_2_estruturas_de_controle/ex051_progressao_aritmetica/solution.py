def progressao_aritmetica(primeiro_termo: int, razao: int, n: int = 10) -> list[int]:
    """Gera os primeiros n termos de uma progressão aritmética (PA).

    Args:
        primeiro_termo (int): primeiro termo da PA.
        razao (int): razão (diferença entre termos consecutivos).
        n (int, opcional): quantidade de termos a gerar. Padrão: 10.

    Returns:
        list[int]: lista com os n primeiros termos da PA.
    """
    raise NotImplementedError
