def fatorial(n: int) -> int:
    """Calcula o fatorial de um inteiro não-negativo.

    Args:
        n (int): número inteiro não-negativo.

    Returns:
        int: n! = n * (n-1) * ... * 1. Por convenção, 0! = 1.
    """
    raise NotImplementedError
