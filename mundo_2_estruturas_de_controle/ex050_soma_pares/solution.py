def soma_pares(numeros: list[int]) -> int:
    """Soma apenas os valores pares de uma lista de inteiros.

    Args:
        numeros (list[int]): lista de números inteiros.

    Returns:
        int: soma dos valores pares (ímpares são desconsiderados).
    """
    raise NotImplementedError
