def calcular_tinta(largura: float, altura: float) -> tuple[float, float]:
    """Retorna (area, litros de tinta) da parede.

    Area = largura * altura; litros = area / 2 (cada litro cobre 2 m²).
    """
    raise NotImplementedError